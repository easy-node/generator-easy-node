{
  "name": "<%= slugname %>",
  "repo": "<%= props.homepage %>",
  "description": "<%= props.description %>",
  "version": "0.0.0",
  "keywords": [
    "<%= slugname %>"<% for (var i=0;i<keywords.length;i++) { %>,
    "<%= keywords[i] %>"<% } %>
  ],
  "dependencies": {
  },
  "development": {
  },
  "scripts": [
      "<%= slugname %>.js"
  ],
  "license": "<%= props.license %>"
}